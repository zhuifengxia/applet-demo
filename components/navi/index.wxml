<view class="container">
    <image bind:tap="onLeft" class="icon left" src="{{latest?disLeftSrc:leftSrc}}" />
    <text class="title">{{title}}</text>
    <image bind:tap="onRight" class="icon right" src="{{first?disRightSrc:rightSrc}}" />
</view>